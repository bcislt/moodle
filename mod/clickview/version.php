<?php

	defined('MOODLE_INTERNAL') || die();

	$plugin->version   = 2018060101;       // The current module version (Date: YYYYMMDDXX)
	$plugin->requires  = 2012062500;       // Requires this Moodle version
	$plugin->component = 'mod_clickview';  // Full name of the plugin (used for diagnostics)
	$plugin->cron      = 0;
